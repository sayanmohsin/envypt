[package]
name = "openv"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Local-first encrypted environment files (SOPS-compatible age)"

[dependencies]
anyhow.workspace = true
openv-core = { path = "../openv-core" }
openv-crypto = { path = "../openv-crypto" }
openv-output = { path = "../openv-output" }
openv-runtime = { path = "../openv-runtime" }
openv-schema = { path = "../openv-schema" }
